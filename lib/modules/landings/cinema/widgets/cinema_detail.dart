import 'package:flutter/material.dart';

class CinemaDetail extends StatelessWidget {
  const CinemaDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
